describe("nextCellStateForCell", () => {
	it("should return the next state for a cell", () => {
		// expect(nextCellStateForCell(0, 0, 0)).toBe(1);
		const temp = 2 / "1"
		expect(temp).toBe(1)
	})
})
